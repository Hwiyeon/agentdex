# Agent Safari

Open the dashboard inside VS Code with:

- `Agent Safari: Open (Watch)`
- `Agent Safari: Open (Mock)`

The extension reuses the same transcript watcher, parser, state model, and public UI as the web dashboard.

Sprite assets are downloaded on demand into VS Code's global storage path.
